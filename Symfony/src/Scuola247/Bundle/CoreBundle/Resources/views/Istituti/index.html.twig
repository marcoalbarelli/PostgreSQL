{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Istituti list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Descrizione</th>
                <th>Codicemeccanografico</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('istituti_show', { 'id': entity.id }) }}">{{ entity.descrizione }}</a></td>
                <td>{{ entity.codicemeccanografico }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('istituti_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('istituti_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('istituti_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
