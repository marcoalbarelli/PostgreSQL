{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Voti list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Descrizione</th>
                <th>Millesimi</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('voti_show', { 'id': entity.id }) }}">{{ entity.descrizione }}</a></td>
                <td>{{ entity.millesimi }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('voti_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('voti_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('voti_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
