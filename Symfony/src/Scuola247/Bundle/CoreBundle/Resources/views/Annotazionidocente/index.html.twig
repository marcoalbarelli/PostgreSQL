{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Annotazionidocente list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Giorno</th>
                <th>Periododilezione</th>
                <th>Descrizione</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('annotazionidocente_show', { 'id': entity.id }) }}">{{ entity.giorno }}</a></td>
                <td>{{ entity.periododilezione }}</td>
                <td>{{ entity.descrizione }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('annotazionidocente_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('annotazionidocente_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('annotazionidocente_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
