{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Librettipersonalimessaggiletti</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Lettoil</th>
                <td>{{ entity.lettoil|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('librettipersonalimessaggiletti') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('librettipersonalimessaggiletti_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('librettipersonalimessaggiletti_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
