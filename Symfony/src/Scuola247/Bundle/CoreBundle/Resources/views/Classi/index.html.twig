{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Classi list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Sezione</th>
                <th>Annodicorso</th>
                <th>Descrizione</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('classi_show', { 'id': entity.id }) }}">{{ entity.sezione }}</a></td>
                <td>{{ entity.annodicorso }}</td>
                <td>{{ entity.descrizione }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('classi_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('classi_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('classi_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
