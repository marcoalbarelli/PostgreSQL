{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nazioni</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Isoa2</th>
                <td>{{ entity.isoa2 }}</td>
            </tr>
            <tr>
                <th>Isoa3</th>
                <td>{{ entity.isoa3 }}</td>
            </tr>
            <tr>
                <th>Descrizione</th>
                <td>{{ entity.descrizione }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('nazioni') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('nazioni_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('nazioni_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
