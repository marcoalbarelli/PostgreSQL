{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Librettipersonalimessaggi creation</h1>

    <form action="{{ path('librettipersonalimessaggi_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('librettipersonalimessaggi') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
