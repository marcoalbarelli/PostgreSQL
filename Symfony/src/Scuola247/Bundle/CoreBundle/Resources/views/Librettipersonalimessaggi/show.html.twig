{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Librettipersonalimessaggi</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Fattoil</th>
                <td>{{ entity.fattoil|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Messaggio</th>
                <td>{{ entity.messaggio }}</td>
            </tr>
            <tr>
                <th>Tipomessaggio</th>
                <td>{{ entity.tipomessaggio }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('librettipersonalimessaggi') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('librettipersonalimessaggi_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('librettipersonalimessaggi_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
