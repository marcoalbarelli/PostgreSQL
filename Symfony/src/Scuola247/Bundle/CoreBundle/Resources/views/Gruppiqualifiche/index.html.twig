{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Gruppiqualifiche list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Descrizione</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('gruppiqualifiche_show', { 'id': entity.id }) }}">{{ entity.descrizione }}</a></td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('gruppiqualifiche_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('gruppiqualifiche_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('gruppiqualifiche_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
