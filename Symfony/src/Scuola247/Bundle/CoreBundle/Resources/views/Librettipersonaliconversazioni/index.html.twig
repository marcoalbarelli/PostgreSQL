{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Librettipersonaliconversazioni list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Oggetto</th>
                <th>Tipoconversazione</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('librettipersonaliconversazioni_show', { 'id': entity.id }) }}">{{ entity.oggetto }}</a></td>
                <td>{{ entity.tipoconversazione }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('librettipersonaliconversazioni_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('librettipersonaliconversazioni_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('librettipersonaliconversazioni_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
