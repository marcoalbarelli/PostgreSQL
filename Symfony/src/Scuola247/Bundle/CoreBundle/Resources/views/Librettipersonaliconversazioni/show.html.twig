{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Librettipersonaliconversazioni</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Oggetto</th>
                <td>{{ entity.oggetto }}</td>
            </tr>
            <tr>
                <th>Tipoconversazione</th>
                <td>{{ entity.tipoconversazione }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('librettipersonaliconversazioni') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('librettipersonaliconversazioni_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('librettipersonaliconversazioni_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
