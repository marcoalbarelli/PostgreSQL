{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Qualifiche list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Annodicorso</th>
                <th>Nome</th>
                <th>Descrizione</th>
                <th>Tipo</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('qualifiche_show', { 'id': entity.id }) }}">{{ entity.annodicorso }}</a></td>
                <td>{{ entity.nome }}</td>
                <td>{{ entity.descrizione }}</td>
                <td>{{ entity.tipo }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('qualifiche_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('qualifiche_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('qualifiche_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
